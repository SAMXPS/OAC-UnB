for inst_name in $(cat to_be_implemented); 
do
echo "class IE_$inst_name : public InstructionExecutor {
    public:
        IE_$s() : InstructionExecutor(0x00,0x00,0x00) { }
        std::string getName() {
            return \"$inst_name\";
        }

        void describe(Instruction* instruction) {
            printf(\"$inst_name\");
        }

        std::string execute(Instruction* instruction, RiscV* cpu) {

        }
};

"
done
